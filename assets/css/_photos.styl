photograph
  center()
  width: 90vw;

  img
    center(100%)

.blur-up {
  -webkit-filter: blur(5px);
  filter: blur(5px);
  transition: filter 400ms, -webkit-filter 400ms;
  width: 90vw;
}

.blur-up.lazyloaded {
  -webkit-filter: blur(0);
  filter: blur(0);
}

description
  p
    blockquote()

    +above(575px)
      margin-left: 5%;

    +above(1600px)
      margin-left: 0;